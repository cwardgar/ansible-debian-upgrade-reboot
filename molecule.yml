---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-debian-upgrade-reboot
      image: ubuntu
      image_version: latest
      ansible_groups:
        - group1
verifier:
  name: testinfra
